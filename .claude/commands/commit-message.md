---
description: create a commit by analyzing git diffs
---

## Your task:

Analyze above staged git changes and create a commit message. Use present tense and explain "why" somwthing has changed, not just "what" has changed.

## Run these commands

```bash
git status
git diff --staged
```
